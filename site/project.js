const root=document.querySelector('#detail');const id=new URLSearchParams(location.search).get('id');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const money=v=>v==null?'—':new Intl.NumberFormat('cs-CZ',{style:'currency',currency:'CZK',maximumFractionDigits:0}).format(v);async function run(){if(!id){root.innerHTML='<div class="empty">Chybí identifikátor zakázky.</div>';return}try{const p=await fetch(`../data/projects/${encodeURIComponent(id)}.json`).then(r=>r.json());const c=p.canonical||{},f=c.financial||{},a=p.analysis||{},events=c.lifecycle?.events||[];root.innerHTML=`<p class="eyebrow">DETAIL ZAKÁZKY</p><h1>${esc(p.title||c.title)}</h1><div class="stats"><div class="stat"><strong>${money(f.initial_contract_price)}</strong><span>výchozí pozorovaná cena</span></div><div class="stat"><strong>${money(f.latest_observed_price)}</strong><span>poslední pozorovaná cena</span></div><div class="stat"><strong>${f.addendum_count||0}</strong><span>dodatků</span></div><div class="stat"><strong>${c.source_count||0}</strong><span>zdrojů</span></div></div><section class="card"><h2>Finanční mapa</h2>${finance(f,events)}</section><section class="card"><h2>Časová osa</h2>${timeline(events)}</section><section class="card"><h2>Kontrolní signály</h2>${signals(a.signals||[])}</section><section class="card"><h2>Zdroje a dokumentace</h2>${sources(p.sources||[])}</section>`}catch(e){root.innerHTML='<div class="empty">Detail se nepodařilo načíst.</div>'}}function finance(f,es){const prices=es.filter(e=>e.price!=null);if(!prices.length)return'<p class="meta">Pro finanční mapu zatím není k dispozici dostatek cenových údajů.</p>';return`<div class="finance-map">${prices.map((e,i)=>`<div class="finance-step"><span>${i===0?'Výchozí':esc(e.type||'Změna')}</span><strong>${money(e.price)}</strong><small>${esc(e.date||'Datum neuvedeno')}</small>${i?`<em>${change(prices[i-1].price,e.price)}</em>`:''}</div>`).join('')}</div><p class="meta">Mapa zobrazuje pouze ceny skutečně nalezené v evidovaných zdrojích. Nejde o právní ani účetní závěr.</p>`}function change(a,b){if(!a)return'';const d=b-a,p=a?d/a*100:0;return`${d>=0?'+':''}${money(d)} (${p>=0?'+':''}${p.toFixed(1)} %)`}function timeline(es){if(!es.length)return'<p class="meta">Časová osa zatím nemá dostatek dat.</p>';return'<div class="timeline">'+es.map(e=>`<div class="timeline-item"><div class="dot"></div><div><strong>${esc(e.date||'Datum neuvedeno')}</strong><span class="badge">${esc(e.type||'záznam')}</span><p>${esc(e.title||'')}${e.price!=null?` · <strong>${money(e.price)}</strong>`:''}</p></div></div>`).join('')+'</div>'}function signals(ss){if(!ss.length)return'<p class="meta">Žádný kontrolní signál.</p>';return ss.map(s=>`<div class="signal"><span class="badge">${esc(s.level||'info')}</span><strong>${esc(s.type||'kontrola')}</strong><p>${esc(s.message||s.description||'')}</p></div>`).join('')}function sources(ss){if(!ss.length)return'<p class="meta">Zdroje zatím nejsou evidovány.</p>';return ss.map(s=>`<div class="source"><strong>${esc(s.source||'zdroj')}</strong><p>${esc(s.title||'')}</p>${s.source_url?`<a href="${esc(s.source_url)}" target="_blank" rel="noopener">Otevřít zdroj →</a>`:''}</div>`).join('')}run();
