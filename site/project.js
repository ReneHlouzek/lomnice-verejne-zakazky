const root=document.querySelector('#detail');const id=new URLSearchParams(location.search).get('id');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const money=v=>v==null?'—':new Intl.NumberFormat('cs-CZ',{style:'currency',currency:'CZK',maximumFractionDigits:0}).format(v);async function run(){if(!id){root.innerHTML='<div class="empty">Chybí identifikátor zakázky.</div>';return}try{const p=await fetch(`../data/projects/${encodeURIComponent(id)}.json`).then(r=>r.json());const c=p.canonical||{},f=c.financial||{},a=p.analysis||{};root.innerHTML=`<p class="eyebrow">DETAIL ZAKÁZKY</p><h1>${esc(p.title||c.title)}</h1><div class="stats"><div class="stat"><strong>${money(f.initial_contract_price)}</strong><span>výchozí pozorovaná cena</span></div><div class="stat"><strong>${money(f.latest_observed_price)}</strong><span>poslední pozorovaná cena</span></div><div class="stat"><strong>${f.addendum_count||0}</strong><span>dodatků</span></div><div class="stat"><strong>${c.source_count||0}</strong><span>zdrojů</span></div></div><section class="card"><h2>Časová osa</h2>${timeline(c.lifecycle?.events||[])}</section><section class="card"><h2>Finanční vývoj</h2><p>Změna mezi výchozí a poslední pozorovanou cenou: <strong>${money(f.absolute_change)}</strong> ${f.percent_change!=null?`(${f.percent_change.toFixed(1)} %)` :''}</p><p class="meta">Údaj je založen pouze na dostupných záznamech; absence ceny neznamená nulovou hodnotu.</p></section><section class="card"><h2>Kontrolní signály</h2>${signals(a.signals||[])}</section><section class="card"><h2>Zdroje</h2>${sources(p.sources||[])}</section>`}catch(e){root.innerHTML='<div class="empty">Detail se nepodařilo načíst.</div>'}}function timeline(es){if(!es.length)return'<p class="meta">Časová osa zatím nemá dostatek dat.</p>';return'<div>'+es.map(e=>`<div style="padding:12px 0;border-bottom:1px solid #e8edf1"><strong>${esc(e.date||'Datum neuvedeno')}</strong> · ${esc(e.type||'záznam')}<br>${esc(e.title||'')} ${e.price!=null?`<strong>${money(e.price)}</strong>`:''}</div>`).join('')+'</div>'}function signals(ss){if(!ss.length)return'<p class="meta">Žádný kontrolní signál.</p>';return ss.map(s=>`<div style="padding:12px 0;border-bottom:1px solid #e8edf1"><span class="badge">${esc(s.level||'info')}</span> <strong>${esc(s.type||'kontrola')}</strong><br>${esc(s.message||s.description||'')}</div>`).join('')}function sources(ss){if(!ss.length)return'<p class="meta">Zdroje zatím nejsou evidovány.</p>';return ss.map(s=>`<p><strong>${esc(s.source||'zdroj')}</strong> · ${esc(s.title||'')}<br>${s.source_url?`<a href="${esc(s.source_url)}" target="_blank" rel="noopener">Otevřít zdroj</a>`:''}</p>`).join('')}run();
