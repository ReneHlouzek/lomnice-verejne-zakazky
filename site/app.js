const $=s=>document.querySelector(s);let index=null;const money=v=>v==null?'—':new Intl.NumberFormat('cs-CZ',{style:'currency',currency:'CZK',maximumFractionDigits:0}).format(v);async function load(){try{index=await fetch('../data/index.json').then(r=>r.json())}catch(e){$('#projects').innerHTML='<div class="empty">Data zatím nejsou dostupná.</div>';return}render();}function render(){const projects=index.projects||[];const q=($('#search').value||'').toLowerCase().trim(),st=$('#status').value;const opts=index.statuses||{};const cov=index.coverage||{};$('#stats').innerHTML=`<div class="stat"><strong>${index.total_projects||0}</strong><span>evidovaných projektů</span></div><div class="stat"><strong>${cov.vhodne_uverejneni_seed_loaded||0} / ${cov.vhodne_uverejneni_profile_total||0}</strong><span>načteno / VU profil</span></div><div class="stat"><strong>${cov.registr_smluv_loaded||0} / ${cov.registr_smluv_organization_contracts||0}</strong><span>Registr smluv / org. stránka</span></div><div class="stat"><strong>${index.counts?.['aktualni-uverejneni']||0}</strong><span>aktuálních uveřejnění</span></div><div class="stat"><strong>${index.counts?.['plneni-smlouvy']||0}</strong><span>plnění smlouvy</span></div><div class="stat"><strong>${index.counts?.['ukonceno-plneni']||0}</strong><span>ukončených plnění</span></div>`;const filtered=projects.filter(p=>(!st||p.status===st)&&(!q||`${p.title} ${p.id} ${p.supplier_ico||''}`.toLowerCase().includes(q)));$('#result-count').textContent=`${filtered.length} z ${projects.length}`;$('#projects').innerHTML=filtered.length?filtered.map(p=>`<a class="card project-card" href="project.html?id=${encodeURIComponent(p.id)}"><h3>${escapeHtml(p.title||'Bez názvu')}</h3><div class="meta"><span class="badge">${escapeHtml(opts[p.status]?.label||'Neroztříděno')}</span><span>${p.source_count||0} zdrojů</span></div></a>`).join(''):'<div class="empty">Žádná zakázka neodpovídá filtru.</div>'}function escapeHtml(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}$('#search').addEventListener('input',render);$('#status').addEventListener('change',render);$('#status').addEventListener('focus',()=>{if($('#status').options.length===1){const opts=index?.statuses||{};$('#status').innerHTML='<option value="">Všechny stavy</option>'+Object.entries(opts).map(([k,v])=>`<option value="${k}">${escapeHtml(v.label||k)}</option>`).join('')}});load();
